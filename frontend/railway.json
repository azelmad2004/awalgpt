{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "installCommand": "npm install --package-lock=false"
  },
  "deploy": {
    "startCommand": "npx serve -s build -l tcp://backend-production-72c3.up.railway.app:$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
